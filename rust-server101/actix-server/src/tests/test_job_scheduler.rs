use crate::classes::job_scheduler::JobScheduler;

#[cfg(test)]
mod tests_job_scheduler {
    use super::*;

    #[test]
    fn test_job_scheduler_creation() {
        let scheduler = JobScheduler::new();
        // Add assertions or logic to test JobScheduler
        assert!(true); // Example assertion
    }
}
